<%= turbo_stream.prepend dom_id(@commentable, :comments), partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.replace "new_comment_form", partial: "comments/form", locals: { commentable: @commentable, placeholder: @placeholder } %>
